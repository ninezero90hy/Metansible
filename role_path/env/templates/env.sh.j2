{% for item in env_sh %}
  
if grep -q "^export {{ item.key }}" /etc/profile; then
    echo "User does exist!!"
else
    echo -e "export {{ item.key }}={{ item.value }}" >> /etc/profile;
fi

{% endfor %}

source /etc/profile;
