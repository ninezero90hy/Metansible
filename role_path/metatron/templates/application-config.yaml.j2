spring:
  profiles: metansible
  datasource:
    url: jdbc:mysql://localhost:3306/polaris?useUnicode=true&amp;characterEncoding=utf8
    username: polaris
    password: polaris
server:
  port: 8180
logging:
  config: classpath:logback-console.xml
polaris:
  engine:
    hostname:
      broker: "{{ DRUID_URL }}:8082"
      overlord: "{{ DRUID_URL }}:8090"
      coordinator: "{{ DRUID_URL }}:8081"
    ingestion:
      baseDir: /tmp
      hosts:
        "{{ HIVE_URL }}":
          port: 22
          username: hive
          password: hive
    query:
      localResultDir: ${java.io.tmpdir:-/tmp}
      defaultForwardUrl: local:/tmp
      hosts:
        localhost:
          port: 22
          username: abc
          password: test